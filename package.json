{
  "name": "ai-cv-maker",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered CV and cover letter generator",
  "scripts": {
    "dev": "concurrently \"pnpm --filter web dev\" \"cd apps/api && poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000\"",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "cd apps/api && poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
